/*
Purpose				: Changes the column names in the HTML X+ Setup Sheet
Mastercam version	: 2019
Date				: Apr 2019
Filename			: default_2019.js
Author				: Thad Seaver
Email				: thad@thadseaver.com

X+ can be downloaded from the German Mastercam reseller GMCCS at:
http://gmccs.de/downloads_x+.php
*/

window.onload = function(){
	
		var colNames = document.getElementsByTagName('th');

		for (var i = 0; i < colNames.length; i++) {

			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Assembly name', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Comment', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Coolant', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Cut angle', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Cutter comp', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Cycle time', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Feed plane', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Feedrate XY', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Feedrate Z-', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Feedrate Z+', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Filter', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Filter ratio', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Groupname', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Holder (new)', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Holder (old)', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Holder length', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Initheight', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Insert', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Op No.', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Operation type', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Overall tolerance', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Prog No.', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Progname', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Retr. plane', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Spindle speed', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Stepdown', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Stepover', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Stock XY', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Stock Z', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl #', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl BMP', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl chuck', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl corner rad', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl Cutlength', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl D#', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl diam.', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl flutes', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl L#', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl length', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl manufactor', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl Mfg code', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl name', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl shldr length', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl tip', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tl type', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tolerance', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Top of stock', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tplane comment', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tplane name', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Tplane No.', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('WCS comment', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('WCS name', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Workoffset No.', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Z-Min', 'Your text here');
			//colNames[i].innerHTML = colNames[i].innerHTML.replace('Z-Max', 'Your text here');
		
		}		
}; /* end script */